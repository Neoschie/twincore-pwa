import Link from "next/link";

type Props = {
  displayName?: string;
  messages: any[];
  isThinking: boolean;
  onStateCheck: () => void;
  showActionButtons: boolean;
};


export function TwinMeChat({
  displayName,
  messages,
  isThinking,
  onStateCheck,
  showActionButtons,
}: Props) {

return(

<div>

{/* Chat Intelligence */}

</div>

);

}